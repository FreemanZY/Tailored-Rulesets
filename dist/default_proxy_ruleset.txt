# RULE-SET: Default Gate targets; FINAL in the profile remains the true catch-all.
# Rules omit the policy field; DNS mappings only use domain entries.
# Intentionally empty. Add reviewed entries only.
